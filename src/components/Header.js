import React from 'react';

const Header = (props) => (
    <div>
        <nav className="navbar navbar-dark bg-dark fixed-top">
            <div className='container'>
                <a className="navbar-brand" href='#'>Empty Classrooms</a>
            </div>
        </nav>
    </div>
);

export default Header;